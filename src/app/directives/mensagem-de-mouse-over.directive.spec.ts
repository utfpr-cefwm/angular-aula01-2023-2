import { MensagemDeMouseOverDirective } from './mensagem-de-mouse-over.directive';

describe('MensagemDeMouseOverDirective', () => {
  it('should create an instance', () => {
    const directive = new MensagemDeMouseOverDirective();
    expect(directive).toBeTruthy();
  });
});
